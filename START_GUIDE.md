# 🚀 Afiya Zone - دليل البدء السريع

## ✅ الحل لمشكلة تسجيل الدخول و MongoDB

### 📋 المشاكل التي تم حلها:

1. **الخادم لم يكن يعمل** - تم تشغيله الآن
2. **MongoDB Warnings** - تم إزالة الخيارات القديمة
3. **متغيرات البيئة** - تم إنشاء ملف `.env` للـ Frontend

---

## 🔧 خطوات التشغيل

### ⚡ الطريقة السريعة (مستحسنة)

افتح PowerShell في مجلد المشروع وقم بتشغيل:

```powershell
.\start-project.ps1
```

هذا السكريبت سيقوم بـ:
- ✅ تشغيل الخادم (Backend)
- ✅ تشغيل التطبيق (Frontend)
- ✅ فتح صفحة الاختبار
- ✅ فتح التطبيق في المتصفح

### 📝 الطريقة اليدوية

### 1️⃣ تشغيل الخادم (Backend)

افتح Terminal جديد وقم بتشغيل:

```powershell
cd "d:\Afiya Zone Store\server"
npm run dev
```

يجب أن ترى:
```
✅ MongoDB Connected: ...
🚀 Server running on port 5000
```

### 2️⃣ تشغيل التطبيق (Frontend)

افتح Terminal آخر وقم بتشغيل:

```powershell
cd "d:\Afiya Zone Store"
npm run dev
```

### 3️⃣ اختبار تسجيل الدخول

افتح الملف التالي في المتصفح:
```
d:\Afiya Zone Store\test-api.html
```

أو استخدم التطبيق مباشرة على:
```
http://localhost:3000
```

---

## 🧪 اختبار الـ API

### طريقة 1: استخدام صفحة الاختبار

1. افتح `test-api.html` في المتصفح
2. اضغط على "Test Connection" - يجب أن يظهر ✅
3. اضغط على "Register User" - سيتم إنشاء مستخدم جديد
4. اضغط على "Login" - سيتم تسجيل الدخول
5. اضغط على "Get All Users" - سيعرض جميع المستخدمين في MongoDB

### طريقة 2: استخدام التطبيق

1. افتح `http://localhost:5173`
2. اضغط على زر Login/Register
3. قم بالتسجيل أو تسجيل الدخول
4. يجب أن يعمل بدون مشاكل

---

## 🔍 التحقق من MongoDB

### عرض جميع المستخدمين:

```
GET http://localhost:5000/api/auth/users
```

### التحقق من اتصال الخادم:

```
GET http://localhost:5000/api/health
```

---

## 📝 معلومات مهمة

### بيانات الاتصال بـ MongoDB:
- **Database**: MongoDB Atlas
- **Connection**: mongodb+srv://afiyaZone:afiya9725@cluster0.s7qxoac.mongodb.net/

### بيانات الاختبار:
- **Email**: test@afiyazone.com
- **Password**: test123456

---

## ⚠️ ملاحظات مهمة

1. **يجب تشغيل الخادم أولاً** قبل استخدام التطبيق
2. **تأكد من تشغيل MongoDB** - الاتصال موجود في ملف `.env`
3. **في حالة وجود مشكلة**:
   - أعد تشغيل الخادم
   - تحقق من الـ Terminal للأخطاء
   - افتح صفحة `test-api.html` للاختبار

---

## 🐛 استكشاف الأخطاء

### المشكلة: "Cannot connect to MongoDB"
**الحل**: تحقق من الاتصال بالإنترنت، MongoDB Atlas يحتاج اتصال نشط

### المشكلة: "Invalid credentials"
**الحل**: تأكد من البريد الإلكتروني وكلمة المرور الصحيحة

### المشكلة: "User already exists"
**الحل**: استخدم بريد إلكتروني مختلف أو قم بتسجيل الدخول بدلاً من التسجيل

---

## 📞 دعم إضافي

إذا واجهت أي مشاكل:
1. افتح Developer Tools في المتصفح (F12)
2. تحقق من تبويب Console للأخطاء
3. تحقق من تبويب Network لطلبات الـ API
4. افتح Terminal وشاهد رسائل الخادم

---

تم إنشاء هذا الدليل لحل مشكلة تسجيل الدخول و MongoDB ✅
