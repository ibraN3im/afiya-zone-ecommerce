# دليل إعداد MongoDB Atlas - خطوة بخطوة 🗄️

## نظرة عامة
MongoDB Atlas هي خدمة قاعدة بيانات سحابية مجانية من MongoDB. سنستخدمها لتخزين بيانات المنتجات والمستخدمين والطلبات.

---

## 📋 الخطوة 1: إنشاء حساب MongoDB Atlas

1. **انتقل إلى موقع MongoDB Atlas**:
   - افتح المتصفح واذهب إلى: https://www.mongodb.com/cloud/atlas/register

2. **أنشئ حساب جديد**:
   - أدخل بريدك الإلكتروني
   - اختر كلمة مرور قوية
   - أو سجل الدخول باستخدام Google

3. **أكمل التسجيل**:
   - وافق على الشروط والأحكام
   - انقر على "Create your Atlas account"

---

## 🏗️ الخطوة 2: إنشاء Cluster مجاني

1. **بعد تسجيل الدخول**، ستظهر لك صفحة الترحيب:
   - انقر على **"Build a Database"** أو **"Create"**

2. **اختر نوع الـ Cluster**:
   - اختر **"Shared"** (المجانية)
   - انقر على **"Create"** تحت خطة M0 Sandbox (FREE)

3. **اختر المنطقة الجغرافية** (Cloud Provider & Region):
   - **Provider**: اختر AWS أو Google Cloud
   - **Region**: اختر أقرب منطقة لك (مثل: Europe/Frankfurt للشرق الأوسط)
   - انقر على **"Create Cluster"**

4. **انتظر قليلاً**:
   - سيستغرق إنشاء الـ Cluster حوالي 1-3 دقائق

---

## 👤 الخطوة 3: إنشاء مستخدم قاعدة البيانات

1. **في قسم Security**، اختر **"Database Access"**

2. **انقر على "Add New Database User"**

3. **أدخل بيانات المستخدم**:
   - **Authentication Method**: اختر Password
   - **Username**: مثل `afiyazone_user`
   - **Password**: أنشئ كلمة مرور قوية (احفظها!)
     - يمكنك استخدام "Autogenerate Secure Password"
   
4. **Database User Privileges**:
   - اختر **"Read and write to any database"**

5. **انقر على "Add User"**

---

## 🌐 الخطوة 4: السماح بالوصول من أي مكان

1. **في قسم Security**، اختر **"Network Access"**

2. **انقر على "Add IP Address"**

3. **خيارات الوصول**:
   
   **للتطوير المحلي والاستضافة المجانية** (موصى به):
   - انقر على **"Allow Access from Anywhere"**
   - سيضيف IP: `0.0.0.0/0`
   - أدخل وصف: `Allow all for development`
   - انقر على **"Confirm"**

   **للإنتاج** (أكثر أماناً):
   - أضف عنوان IP الخاص بخادم الاستضافة فقط

---

## 🔗 الخطوة 5: الحصول على Connection String

1. **ارجع إلى Database**:
   - انقر على **"Database"** في القائمة الجانبية

2. **انقر على "Connect"** بجانب اسم الـ Cluster

3. **اختر طريقة الاتصال**:
   - انقر على **"Connect your application"**

4. **اختر Driver**:
   - **Driver**: Node.js
   - **Version**: 4.1 or later

5. **انسخ Connection String**:
   ```
   mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```

6. **عدّل Connection String**:
   - استبدل `<username>` باسم المستخدم الذي أنشأته
   - استبدل `<password>` بكلمة المرور
   - أضف اسم قاعدة البيانات قبل `?`

   **مثال نهائي**:
   ```
   mongodb+srv://afiyazone_user:MyPassword123@cluster0.xxxxx.mongodb.net/afiyazone?retryWrites=true&w=majority
   ```

---

## ⚙️ الخطوة 6: إضافة Connection String إلى المشروع

1. **افتح مجلد server**

2. **أنشئ ملف `.env`**:
   ```bash
   cd server
   cp .env.example .env
   ```

3. **افتح ملف `.env` وأضف**:
   ```env
   MONGODB_URI=mongodb+srv://afiyazone_user:MyPassword123@cluster0.xxxxx.mongodb.net/afiyazone?retryWrites=true&w=majority
   JWT_SECRET=my_super_secret_key_12345
   PORT=5000
   NODE_ENV=development
   ```

4. **احفظ الملف**

---

## ✅ الخطوة 7: اختبار الاتصال

1. **من مجلد server، شغّل**:
   ```bash
   npm install
   ```

2. **أضف المنتجات إلى قاعدة البيانات**:
   ```bash
   node seedProducts.js
   ```

3. **إذا نجحت العملية، ستظهر**:
   ```
   ✅ MongoDB Connected: cluster0-xxxxx.mongodb.net
   🗑️  Cleared existing products
   ✅ Products seeded successfully
   ```

4. **شغّل الخادم**:
   ```bash
   npm run dev
   ```

5. **ستظهر رسالة**:
   ```
   ✅ MongoDB Connected: cluster0-xxxxx.mongodb.net
   🚀 Server running on port 5000
   ```

---

## 🔍 الخطوة 8: التحقق من البيانات في Atlas

1. **ارجع إلى موقع MongoDB Atlas**

2. **انقر على "Database" ثم "Browse Collections"**

3. **يجب أن ترى**:
   - Database: `afiyazone`
   - Collections: `products`, `users`, `orders`
   - 6 منتجات في collection الـ `products`

---

## 🛠️ استكشاف الأخطاء

### خطأ: "MongoServerError: bad auth"
**الحل**:
- تأكد من username و password صحيحين
- تأكد من عدم وجود رموز خاصة في كلمة المرور
- إذا كانت كلمة المرور تحتوي على رموز خاصة، استخدم URL encoding

### خطأ: "MongooseServerSelectionError"
**الحل**:
- تأكد من إضافة `0.0.0.0/0` في Network Access
- تحقق من الاتصال بالإنترنت

### خطأ: "Database not found"
**الحل**:
- تأكد من إضافة اسم قاعدة البيانات في connection string
- يجب أن يكون: `.../afiyazone?retryWrites=...`

---

## 📊 معلومات مهمة

### حدود الخطة المجانية M0:
- ✅ **مساحة التخزين**: 512 MB (كافية لآلاف المنتجات)
- ✅ **مشترك**: مجاني مدى الحياة
- ✅ **Backups**: يومية تلقائية
- ⚠️ **Connections**: 500 اتصال متزامن

### نصائح الأمان:
1. **لا تشارك** connection string مع أحد
2. **لا ترفع** ملف `.env` إلى GitHub
3. **استخدم** كلمات مرور قوية
4. **في الإنتاج**، استخدم IP whitelist محددة

---

## 🎉 تهانينا!

الآن لديك قاعدة بيانات MongoDB Atlas جاهزة ومتصلة بمشروعك!

### الخطوات التالية:
1. ✅ قاعدة البيانات جاهزة
2. ✅ المنتجات مضافة
3. ✅ الخادم يعمل
4. ⏭️ شغّل الـ Frontend واختبر التطبيق

---

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من رسائل الخطأ في Terminal
2. راجع connection string
3. تأكد من Network Access مضبوطة
4. تأكد من بيانات المستخدم صحيحة

---

**ملاحظة**: احفظ username و password و connection string في مكان آمن!
